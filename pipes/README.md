# Pipes

## 學習內容
### angular pipes
- 使用 `{{ value | pipe: 'arg'}}`
- decimal pipe: ```integar.minfloat-maxfloat```
### custom pipe
- ```ng g pipe <pipe name>```
- ```transform (value,arg)```: arg是```pipe:```後面接的參數
